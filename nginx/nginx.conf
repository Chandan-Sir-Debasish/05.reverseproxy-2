events {
    worker_connections 1024;
}

http {
    map $http_upgrade $connection_upgrade {
        default upgrade;
        ''      close;
    }

    upstream node_backend {
        least_conn;
        server backend-node-1:3000 weight=3 max_fails=3 fail_timeout=30s;
        server backend-node-2:3001 weight=2 max_fails=3 fail_timeout=30s;
        keepalive 32;
    }

    upstream python_backend {
        server backend-python:5000 max_fails=3 fail_timeout=30s;
    }

    upstream java_backend {
        server backend-java:8080;
    }

    server {
        listen 80;
        server_name localhost;

        add_header X-Frame-Options "SAMEORIGIN" always;
        add_header X-Content-Type-Options "nosniff" always;
        add_header X-XSS-Protection "1; mode=block" always;

        access_log /var/log/nginx/access.log;
        error_log /var/log/nginx/error.log;

        # Redirect non-trailing slash routes to trailing slash versions
        rewrite ^/api/node$ /api/node/ permanent;
        rewrite ^/api/python$ /api/python/ permanent;
        rewrite ^/api/java$ /api/java/ permanent;

        location / {
            add_header Content-Type text/plain;
            return 200 "Welcome to API Gateway\n";
        }

        location /api/node/ {
            proxy_pass http://node_backend/;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection $connection_upgrade;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_cache_bypass $http_upgrade;
            proxy_connect_timeout 60s;
            proxy_send_timeout 60s;
            proxy_read_timeout 60s;
        }

        location /api/python/ {
            proxy_pass http://python_backend/;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            limit_req zone=mylimit burst=20 nodelay;
        }

        location /api/java/ {
            proxy_pass http://java_backend/;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        location /health {
            access_log off;
            add_header Content-Type text/plain;
            return 200 "healthy\n";
        }

        location /nginx_status {
            stub_status on;
            access_log off;
            allow 127.0.0.1;
            deny all;
        }
    }

    limit_req_zone $binary_remote_addr zone=mylimit:10m rate=10r/s;
}